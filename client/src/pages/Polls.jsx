import React from 'react';
import { Box, Typography } from '@mui/material';

const Polls = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" color="text.primary">Polls</Typography>
      <Typography color="text.secondary">Polls page placeholder</Typography>
    </Box>
  );
};

export default Polls;
