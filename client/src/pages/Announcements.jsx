import React from 'react';
import { Box, Typography } from '@mui/material';

const Announcements = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" color="text.primary">Announcements</Typography>
      <Typography color="text.secondary">Announcements page placeholder</Typography>
    </Box>
  );
};

export default Announcements;
